diff --git a/dist/cjs/development/debug-C46WDxhL.js b/dist/cjs/development/debug-C46WDxhL.js
index 59b724fc95d38bae5ee00cddcbd19bbd706dc409..c925d34156b10a8dc87e86740877c2ae7d4fc09b 100644
--- a/dist/cjs/development/debug-C46WDxhL.js
+++ b/dist/cjs/development/debug-C46WDxhL.js
@@ -573,8 +573,19 @@ function propagateDirty(signal2) {
 function dirtySignalConsumers(map) {
   for (const [subRef, edge] of map) {
     const sub = subRef.deref();
-    if (sub === void 0 || sub.computedCount !== edge.consumedAt)
+    if (sub === void 0)
       continue;
+    if (sub.computedCount !== edge.consumedAt) {
+      // PATCH (dash-chat): the edge belongs to an older computation because the
+      // subscriber is recomputing right now, and that run started before this
+      // change — dropping the notification leaves it holding the previous
+      // value for good. Mark it dirty so it runs again once it settles.
+      // State 2 is used deliberately: it needs no dirtyHead, which is what
+      // makes the 1/3/4 branch below crash when there is none.
+      sub._state = 2;
+      scheduleAsyncPull(sub);
+      continue;
+    }
     const dirtyState = sub._state;
     switch (dirtyState) {
       case 0:
@@ -769,24 +780,13 @@ function createCallback(fn, scope, deps) {
   return new Callback(fn, scope, deps);
 }
 function callback(fn, idx, deps) {
-  let callbacks;
-  if (CURRENT_CALLBACK !== void 0) {
-    callbacks = CURRENT_CALLBACK.callbacks;
-  } else {
-    const currentConsumer = getCurrentConsumer();
-    if (currentConsumer === void 0) {
-      throw new Error("callback must be used within a reactive function, component, or nested callback");
-    }
-    callbacks = currentConsumer.callbacks ?? (currentConsumer.callbacks = []);
-  }
-  let callback2 = callbacks[idx];
-  if (callback2 === void 0) {
-    callback2 = callbacks[idx] = createCallback(fn, getCurrentScope(), deps);
-  } else if (deps && callback2.deps?.findIndex((dep, i) => dep !== deps[i]) !== -1) {
-    callback2.setFn(fn);
-    callback2.deps = deps;
+  // PATCH (dash-chat): bypass per-consumer callbacks[idx] caching to avoid
+  // cross-reactive slot collisions observed in 3.0.1. See the development
+  // ESM bundle for the full explanation.
+  if (CURRENT_CALLBACK === void 0 && getCurrentConsumer() === void 0) {
+    throw new Error("callback must be used within a reactive function, component, or nested callback");
   }
-  return callback2.fn;
+  return createCallback(fn, getCurrentScope(), deps).fn;
 }
 const arrayFrom = Array.from;
 function isThenable(v) {
@@ -1069,6 +1069,11 @@ class ReactivePromiseImpl {
     return this._awaitSubs = /* @__PURE__ */ new Map();
   }
   _clearPending() {
+    // PATCH (dash-chat): a ReactivePromise that has never resolved has no value
+    // to re-publish. Resolving it here marks it Ready with `undefined` and hands
+    // that to every awaiter, so a reactive mid-first-computation resolves to
+    // undefined. Stay Pending and let the in-flight computation resolve it.
+    if ((this._flags & 8) === 0) return;
     this._setValue(this._value);
   }
   async _setPromise(promise) {
@@ -1158,7 +1163,11 @@ class ReactivePromiseImpl {
     const newState = dirty ? 2 : 4;
     for (const ref of awaitSubs.keys()) {
       const signal2 = ref.deref();
-      if (signal2 === void 0) {
+      // PATCH (dash-chat): a subscriber that re-ran since checkSignal halted it
+      // is Clean with no dirtyHead; marking it PendingDirty makes the next
+      // dirtySignalConsumers read `dirtyHead.ord` off undefined. If it awaits
+      // us again it resumes through `_pending`, so leave it alone.
+      if (signal2 === void 0 || signal2._state === 0) {
         continue;
       }
       signal2._state = newState;
diff --git a/dist/cjs/production/contexts-CM3QLfV8.js b/dist/cjs/production/contexts-CM3QLfV8.js
index fc90dde43b4d9bdf76d497238448a5cbb0d80a69..3e52f00f8f84bf5789af235992a97e316a7371da 100644
--- a/dist/cjs/production/contexts-CM3QLfV8.js
+++ b/dist/cjs/production/contexts-CM3QLfV8.js
@@ -234,8 +234,19 @@ function propagateDirty(signal2) {
 function dirtySignalConsumers(map) {
   for (const [subRef, edge] of map) {
     const sub = subRef.deref();
-    if (sub === void 0 || sub.computedCount !== edge.consumedAt)
+    if (sub === void 0)
       continue;
+    if (sub.computedCount !== edge.consumedAt) {
+      // PATCH (dash-chat): the edge belongs to an older computation because the
+      // subscriber is recomputing right now, and that run started before this
+      // change — dropping the notification leaves it holding the previous
+      // value for good. Mark it dirty so it runs again once it settles.
+      // State 2 is used deliberately: it needs no dirtyHead, which is what
+      // makes the 1/3/4 branch below crash when there is none.
+      sub._state = 2;
+      scheduleAsyncPull(sub);
+      continue;
+    }
     const dirtyState = sub._state;
     switch (dirtyState) {
       case 0:
@@ -427,24 +438,13 @@ function createCallback(fn, scope, deps) {
   return new Callback(fn, scope, deps);
 }
 function callback(fn, idx, deps) {
-  let callbacks;
-  if (CURRENT_CALLBACK !== void 0) {
-    callbacks = CURRENT_CALLBACK.callbacks;
-  } else {
-    const currentConsumer = getCurrentConsumer();
-    if (currentConsumer === void 0) {
-      throw new Error("callback must be used within a reactive function, component, or nested callback");
-    }
-    callbacks = currentConsumer.callbacks ?? (currentConsumer.callbacks = []);
-  }
-  let callback2 = callbacks[idx];
-  if (callback2 === void 0) {
-    callback2 = callbacks[idx] = createCallback(fn, getCurrentScope(), deps);
-  } else if (deps && callback2.deps?.findIndex((dep, i) => dep !== deps[i]) !== -1) {
-    callback2.setFn(fn);
-    callback2.deps = deps;
+  // PATCH (dash-chat): bypass per-consumer callbacks[idx] caching to avoid
+  // cross-reactive slot collisions observed in 3.0.1. See the development
+  // ESM bundle for the full explanation.
+  if (CURRENT_CALLBACK === void 0 && getCurrentConsumer() === void 0) {
+    throw new Error("callback must be used within a reactive function, component, or nested callback");
   }
-  return callback2.fn;
+  return createCallback(fn, getCurrentScope(), deps).fn;
 }
 const arrayFrom = Array.from;
 function isThenable(v) {
@@ -701,6 +701,11 @@ class ReactivePromiseImpl {
     return this._awaitSubs = /* @__PURE__ */ new Map();
   }
   _clearPending() {
+    // PATCH (dash-chat): a ReactivePromise that has never resolved has no value
+    // to re-publish. Resolving it here marks it Ready with `undefined` and hands
+    // that to every awaiter, so a reactive mid-first-computation resolves to
+    // undefined. Stay Pending and let the in-flight computation resolve it.
+    if ((this._flags & 8) === 0) return;
     this._setValue(this._value);
   }
   async _setPromise(promise) {
@@ -790,7 +795,11 @@ class ReactivePromiseImpl {
     const newState = dirty ? 2 : 4;
     for (const ref of awaitSubs.keys()) {
       const signal2 = ref.deref();
-      if (signal2 === void 0) {
+      // PATCH (dash-chat): a subscriber that re-ran since checkSignal halted it
+      // is Clean with no dirtyHead; marking it PendingDirty makes the next
+      // dirtySignalConsumers read `dirtyHead.ord` off undefined. If it awaits
+      // us again it resumes through `_pending`, so leave it alone.
+      if (signal2 === void 0 || signal2._state === 0) {
         continue;
       }
       signal2._state = newState;
diff --git a/dist/esm/development/debug-BTLbgTPK.js b/dist/esm/development/debug-BTLbgTPK.js
index 59cf1668a2213dae6a6a90a635238d67a33fc96b..6224b2273ed206578889d6fef54a9c563ccb6b94 100644
--- a/dist/esm/development/debug-BTLbgTPK.js
+++ b/dist/esm/development/debug-BTLbgTPK.js
@@ -572,8 +572,19 @@ function propagateDirty(signal2) {
 function dirtySignalConsumers(map) {
   for (const [subRef, edge] of map) {
     const sub = subRef.deref();
-    if (sub === void 0 || sub.computedCount !== edge.consumedAt)
+    if (sub === void 0)
       continue;
+    if (sub.computedCount !== edge.consumedAt) {
+      // PATCH (dash-chat): the edge belongs to an older computation because the
+      // subscriber is recomputing right now, and that run started before this
+      // change — dropping the notification leaves it holding the previous
+      // value for good. Mark it dirty so it runs again once it settles.
+      // State 2 is used deliberately: it needs no dirtyHead, which is what
+      // makes the 1/3/4 branch below crash when there is none.
+      sub._state = 2;
+      scheduleAsyncPull(sub);
+      continue;
+    }
     const dirtyState = sub._state;
     switch (dirtyState) {
       case 0:
@@ -768,24 +779,21 @@ function createCallback(fn, scope, deps) {
   return new Callback(fn, scope, deps);
 }
 function callback(fn, idx, deps) {
-  let callbacks;
-  if (CURRENT_CALLBACK !== void 0) {
-    callbacks = CURRENT_CALLBACK.callbacks;
-  } else {
-    const currentConsumer = getCurrentConsumer();
-    if (currentConsumer === void 0) {
-      throw new Error("callback must be used within a reactive function, component, or nested callback");
-    }
-    callbacks = currentConsumer.callbacks ?? (currentConsumer.callbacks = []);
-  }
-  let callback2 = callbacks[idx];
-  if (callback2 === void 0) {
-    callback2 = callbacks[idx] = createCallback(fn, getCurrentScope(), deps);
-  } else if (deps && callback2.deps?.findIndex((dep, i) => dep !== deps[i]) !== -1) {
-    callback2.setFn(fn);
-    callback2.deps = deps;
-  }
-  return callback2.fn;
+  // PATCH (dash-chat): the original implementation indexes a per-consumer
+  // `callbacks[idx]` array and reuses cached Callback wrappers across
+  // reactive re-runs for closure identity stability. In practice that slot
+  // table has shown cross-reactive collisions in 3.0.1 (one reactive's
+  // callbacks[0] is returned for a different reactive's _callback call,
+  // so Array.sort runs the wrong inner fn). We bypass the cache and
+  // always create a fresh Callback. The wrapping itself — scope tracking
+  // and generator-result-to-Promise conversion — is preserved by
+  // createCallback, so nested async transforms still work. Trade-off:
+  // we lose useCallback-style stable identity across re-runs (irrelevant
+  // for callbacks passed to Array methods).
+  if (CURRENT_CALLBACK === void 0 && getCurrentConsumer() === void 0) {
+    throw new Error("callback must be used within a reactive function, component, or nested callback");
+  }
+  return createCallback(fn, getCurrentScope(), deps).fn;
 }
 const arrayFrom = Array.from;
 function isThenable(v) {
@@ -1068,6 +1076,11 @@ class ReactivePromiseImpl {
     return this._awaitSubs = /* @__PURE__ */ new Map();
   }
   _clearPending() {
+    // PATCH (dash-chat): a ReactivePromise that has never resolved has no value
+    // to re-publish. Resolving it here marks it Ready with `undefined` and hands
+    // that to every awaiter, so a reactive mid-first-computation resolves to
+    // undefined. Stay Pending and let the in-flight computation resolve it.
+    if ((this._flags & 8) === 0) return;
     this._setValue(this._value);
   }
   async _setPromise(promise) {
@@ -1157,7 +1170,11 @@ class ReactivePromiseImpl {
     const newState = dirty ? 2 : 4;
     for (const ref of awaitSubs.keys()) {
       const signal2 = ref.deref();
-      if (signal2 === void 0) {
+      // PATCH (dash-chat): a subscriber that re-ran since checkSignal halted it
+      // is Clean with no dirtyHead; marking it PendingDirty makes the next
+      // dirtySignalConsumers read `dirtyHead.ord` off undefined. If it awaits
+      // us again it resumes through `_pending`, so leave it alone.
+      if (signal2 === void 0 || signal2._state === 0) {
         continue;
       }
       signal2._state = newState;
diff --git a/dist/esm/production/contexts-FxCndlvf.js b/dist/esm/production/contexts-FxCndlvf.js
index d578ce64f32f9970e55d028e4d727d9c76a62697..61d49046b14665e92c7a5af1fa56602e2651f114 100644
--- a/dist/esm/production/contexts-FxCndlvf.js
+++ b/dist/esm/production/contexts-FxCndlvf.js
@@ -233,8 +233,19 @@ function propagateDirty(signal2) {
 function dirtySignalConsumers(map) {
   for (const [subRef, edge] of map) {
     const sub = subRef.deref();
-    if (sub === void 0 || sub.computedCount !== edge.consumedAt)
+    if (sub === void 0)
       continue;
+    if (sub.computedCount !== edge.consumedAt) {
+      // PATCH (dash-chat): the edge belongs to an older computation because the
+      // subscriber is recomputing right now, and that run started before this
+      // change — dropping the notification leaves it holding the previous
+      // value for good. Mark it dirty so it runs again once it settles.
+      // State 2 is used deliberately: it needs no dirtyHead, which is what
+      // makes the 1/3/4 branch below crash when there is none.
+      sub._state = 2;
+      scheduleAsyncPull(sub);
+      continue;
+    }
     const dirtyState = sub._state;
     switch (dirtyState) {
       case 0:
@@ -426,24 +437,13 @@ function createCallback(fn, scope, deps) {
   return new Callback(fn, scope, deps);
 }
 function callback(fn, idx, deps) {
-  let callbacks;
-  if (CURRENT_CALLBACK !== void 0) {
-    callbacks = CURRENT_CALLBACK.callbacks;
-  } else {
-    const currentConsumer = getCurrentConsumer();
-    if (currentConsumer === void 0) {
-      throw new Error("callback must be used within a reactive function, component, or nested callback");
-    }
-    callbacks = currentConsumer.callbacks ?? (currentConsumer.callbacks = []);
-  }
-  let callback2 = callbacks[idx];
-  if (callback2 === void 0) {
-    callback2 = callbacks[idx] = createCallback(fn, getCurrentScope(), deps);
-  } else if (deps && callback2.deps?.findIndex((dep, i) => dep !== deps[i]) !== -1) {
-    callback2.setFn(fn);
-    callback2.deps = deps;
+  // PATCH (dash-chat): bypass per-consumer callbacks[idx] caching to avoid
+  // cross-reactive slot collisions observed in 3.0.1. See the development
+  // bundle for the full explanation.
+  if (CURRENT_CALLBACK === void 0 && getCurrentConsumer() === void 0) {
+    throw new Error("callback must be used within a reactive function, component, or nested callback");
   }
-  return callback2.fn;
+  return createCallback(fn, getCurrentScope(), deps).fn;
 }
 const arrayFrom = Array.from;
 function isThenable(v) {
@@ -700,6 +700,11 @@ class ReactivePromiseImpl {
     return this._awaitSubs = /* @__PURE__ */ new Map();
   }
   _clearPending() {
+    // PATCH (dash-chat): a ReactivePromise that has never resolved has no value
+    // to re-publish. Resolving it here marks it Ready with `undefined` and hands
+    // that to every awaiter, so a reactive mid-first-computation resolves to
+    // undefined. Stay Pending and let the in-flight computation resolve it.
+    if ((this._flags & 8) === 0) return;
     this._setValue(this._value);
   }
   async _setPromise(promise) {
@@ -789,7 +794,11 @@ class ReactivePromiseImpl {
     const newState = dirty ? 2 : 4;
     for (const ref of awaitSubs.keys()) {
       const signal2 = ref.deref();
-      if (signal2 === void 0) {
+      // PATCH (dash-chat): a subscriber that re-ran since checkSignal halted it
+      // is Clean with no dirtyHead; marking it PendingDirty makes the next
+      // dirtySignalConsumers read `dirtyHead.ord` off undefined. If it awaits
+      // us again it resumes through `_pending`, so leave it alone.
+      if (signal2 === void 0 || signal2._state === 0) {
         continue;
       }
       signal2._state = newState;
