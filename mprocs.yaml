procs:
  agent1:
    shell: just dev tauri
  agent2:
    shell: just dev tauri
    autostart: false
  mailbox-server:
    shell: cargo run -p mailbox-server -- --db-path "$PWD/.dbs/dev/mailbox-server/mailbox.db" --addr 0.0.0.0:3000 --push-notifications-url http://localhost:3001
  push-notifications-server:
    shell: just push server
    autostart: false
  android:
    shell: just android dev
    autostart: false
  logcat:
    shell: just android logcat
    autostart: false
  ios:
    shell: just ios dev
    autostart: false
