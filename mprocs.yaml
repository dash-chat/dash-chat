procs:
  agent1:
    shell: DATA_DIR="$PWD/.dbs/dev/agent-1" just dev tauri
  agent2:
    shell: DATA_DIR="$PWD/.dbs/dev/agent-2" just dev tauri
    autostart: false
  mailbox-server:
    shell: just mailbox server
  push-notifications-server:
    shell: just push server
    autostart: false
  android:
    shell: just android dev
    autostart: false
  logcat:
    shell: just android logcat
    autostart: false
  ios:
    shell: just ios dev
    autostart: false
