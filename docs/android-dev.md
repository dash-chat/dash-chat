# Android Development

Run the app in a connected device in development mode:

```bash
just android-dev
```

Seeing the logs for the app running in a connected device:

```bash
just android-dev
```

Building and installing the app:

```bash
just android-install
```

Uninstalling the app:

```bash
just android-uninstall
```
