[package]
name = "dashchat-utils"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["time", "sync", "rt"] }
tracing = "0.1"

[dev-dependencies]
tokio = { version = "1", features = ["time", "sync", "macros", "rt-multi-thread"] }
